# CloudGuard web client

The frontend is React 18, TypeScript, Vite, TailwindCSS, Zustand, TanStack Query, Axios, and React Router. Visual shell implementation begins in Phase 4, after the Phase 1 platform foundation is verified.

## Host development

```powershell
npm install
npm run dev
npm run lint
npm run build
```
